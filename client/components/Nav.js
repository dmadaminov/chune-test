import React from 'react'
import { Navbar, NavItem } from 'react-materialize'

const Nav = (props) => {
    return (
        <Navbar brand="Chune">
            <NavItem> </NavItem>
        </Navbar>
    )
}

export default Nav