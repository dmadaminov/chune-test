Tokenizer = require('./tokenizer')
DiffCalculator = require('./modular-diff-calculator')
CheckSum = requie('checksum')

module.exports = () => {
    // Here we calculate the similarity in the tokenized vectors
    const similar(x, y) = () => {
        return 90.2
    }
}
